#include <vector>
#include <iterator>
#include <iostream>

/*
Implement the following:

equal(b, e, d)
find(b, e, t)
copy(b, e, d)
remove_copy_if(b, e, d, p)
remove(b, e, t)
transform(b, e, d, f)
partition(b, e, p)
accumulate(b, e, t)
*/

template <class In, class Out>
bool equal(In b, In e, Out d)
{
  // TODO: logic
}

template <class In, class T>
bool find(In b, In e, T t)
{
  // TODO: logic
}

template <class In, class Out>
Out copy(In b, In e, Out d)
{
  // TODO: logic
}

int main()
{
}